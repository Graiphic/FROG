<h1>Example 10 UI</h1>

<p>
The UI package for Example 10 references the Default Button realization, the Default
Boolean realization, and their canonical SVG assets. The <code>.frog</code> source owns
the widget instances, layout, value bindings, caption placement, mechanical action,
and per-instance style values.
</p>
