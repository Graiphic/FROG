# Example 41 Reference Screenshot

This directory publishes the screenshot-only public reference for Example 41,
the Numeric placement IDE view.

## Evidence

- `screenshot.png`: captured from the private C++ browser-host runtime on
  `2026-06-22`.
- Visual check: the screenshot was opened after capture and confirmed to show
  the Numeric IDE placement grid, six Numeric widgets, the selected
  `ctrl_input_left` aura, right-side buttons, left-side buttons, hidden-button
  control posture, and Numeric indicator styling.

## Boundary

The screenshot is public human comparison evidence only. It does not publish
private runtime state JSON, visual-contract JSON, artifact hashes, or internal
validation recipes.
